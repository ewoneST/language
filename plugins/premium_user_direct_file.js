//Code for Premium Users

//Direct File

let filter = require('auto-filter');

filter.load('direct-file', {
  type: 'premium_user',
  url: 
}).then(() => {
  // Do something after filter is successfully loaded
});
